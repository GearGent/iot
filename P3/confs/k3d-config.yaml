apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: geargenc
servers: 1
agents: 0
ports:
  - port: 8080:80
    nodeFilters:
      - server:0:direct
options:
  k3d:
    disableLoadbalancer: true
    wait: true
